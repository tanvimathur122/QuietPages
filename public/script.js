const $=id=>document.getElementById(id), modal=$("modal"), cards=$("cards");
let token=localStorage.getItem("qp_token"),currentUser=JSON.parse(localStorage.getItem("qp_user")||"null"),editingId=null,currentPrompt=null;
const categories=["Free Write","Gratitude","Daily Check-in","Memories","Thoughts","Goals"],moods={great:"😊 Great",good:"🙂 Good",okay:"😐 Okay",low:"😔 Low",difficult:"😣 Difficult"};
const esc=s=>String(s??"").replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const date=s=>new Date(s).toLocaleDateString(undefined,{month:"short",day:"numeric",year:"numeric"});
function toast(t){const x=$("toast");x.textContent=t;x.classList.add("show");setTimeout(()=>x.classList.remove("show"),2600)}
function updateNav(){ $("myLink").classList.toggle("hidden",!currentUser);$("adminLink").classList.toggle("hidden",currentUser?.role!=="admin");$("authBtn").textContent=currentUser?"Sign out":"Sign in";$("authBtn").onclick=currentUser?signOut:openAuth; }
function goHome(){location.hash='stories';showHomeSections();document.querySelector('.section-heading .eyebrow').textContent='the quiet community';document.querySelector('.section-heading h2').textContent='Words from here.';document.querySelector('.section-heading p').textContent='Real thoughts. No performance required.';loadDiaries();}
function hideMain(){document.querySelector("#homeHero").classList.add("hidden");document.querySelector(".quick-start").classList.add("hidden");document.querySelector(".privacy-strip").classList.add("hidden");}
function showHomeSections(){document.querySelector("#homeHero").classList.remove("hidden");document.querySelector(".quick-start").classList.remove("hidden");document.querySelector(".privacy-strip").classList.remove("hidden");}
function openModal(html){$("modalContent").innerHTML=html;modal.classList.remove("hidden");}
function closeModal(){modal.classList.add("hidden");editingId=null;currentPrompt=null;}
modal.addEventListener("click",e=>{if(e.target===modal)closeModal()});document.addEventListener("keydown",e=>{if(e.key==="Escape")closeModal()});
function openAuth(){openModal(`<span class="eyebrow">quiet pages</span><h2>Come as you are.</h2><p class="modal-sub">Sign in to keep pages private, save stories and track your writing.</p><div class="tabs"><button class="active" onclick="authForm('login',this)">Sign in</button><button onclick="authForm('signup',this)">Create account</button></div><div id="authForm"></div>`);authForm('login',document.querySelector('.tabs button'))}
function authForm(type,btn){document.querySelectorAll('.tabs button').forEach(x=>x.classList.remove('active'));btn?.classList.add('active');$("authForm").innerHTML=type==='login'?`<div class="form"><label>Email<input id="email" type="email" autocomplete="email"></label><label>Password<input id="password" type="password" autocomplete="current-password"></label><button class="submit-btn" onclick="login()">Sign in</button></div>`:`<div class="form"><label>Your pen name<input id="name" maxlength="40" autocomplete="name"></label><label>Email<input id="email" type="email" autocomplete="email"></label><label>Password<input id="password" type="password" minlength="6" autocomplete="new-password"></label><button class="submit-btn" onclick="signup()">Create my space</button></div>`}
async function login(){const r=await fetch('/api/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:$('email').value,password:$('password').value})}),d=await r.json();if(!r.ok)return toast(d.error);token=d.token;currentUser=d.user;localStorage.setItem('qp_token',token);localStorage.setItem('qp_user',JSON.stringify(currentUser));closeModal();updateNav();toast('Welcome back.');showDashboard()}
async function signup(){const r=await fetch('/api/signup',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:$('name').value,email:$('email').value,password:$('password').value})}),d=await r.json();if(!r.ok)return toast(d.error);token=d.token;currentUser=d.user;localStorage.setItem('qp_token',token);localStorage.setItem('qp_user',JSON.stringify(currentUser));closeModal();updateNav();toast('Your quiet space is ready.');showDashboard()}
function signOut(){token=null;currentUser=null;localStorage.removeItem('qp_token');localStorage.removeItem('qp_user');updateNav();showHomeSections();loadDiaries();toast('Signed out.');}
function openWrite(mode='free',id=null,data=null){if(!currentUser)return openAuth();editingId=id;currentPrompt=data?.prompt||null;const title=data?.title||'';const body=data?.body||'';const selectedMood=data?.mood||'';const selectedCategory=data?.category||(mode==='checkin'?'Daily Check-in':mode==='quick'?'Thoughts':'Free Write');const visibility=data?(data.is_anonymous?'anonymous':data.is_public?'public':'private'):'private';openModal(`<span class="eyebrow">${id?'edit your page':'new page'}</span><h2>${id?'Give it another shape.':'What’s on your mind?'}</h2><p class="modal-sub">There is no right way to journal. A sentence is enough.</p><div class="write-modes"><button onclick="setWriteMode('Free Write')">Free write</button><button onclick="getPrompt()">✦ Prompt me</button><button onclick="setWriteMode('Daily Check-in')">Daily check-in</button></div><div id="promptBox" class="prompt-box ${currentPrompt?'':'hidden'}">${currentPrompt?esc(currentPrompt):''}<button onclick="getPrompt()">Another prompt</button></div><div class="form"><label>Title<input id="diaryTitle" maxlength="100" value="${esc(title)}" placeholder="A small title, if you want one"></label><label>Category<select id="diaryCategory">${categories.map(c=>`<option ${c===selectedCategory?'selected':''}>${c}</option>`).join('')}</select></label><label>How does this page feel?<div class="mood-picker">${Object.entries(moods).map(([k,v])=>`<button type="button" class="mood ${k===selectedMood?'selected':''}" data-mood="${k}" onclick="pickMood('${k}')">${v}</button>`).join('')}</div></label><label>Your words<textarea id="diaryBody" rows="10" maxlength="12000" placeholder="Start wherever you are…">${esc(body)}</textarea></label><div class="visibility"><strong>Who can see this?</strong><label><input type="radio" name="visibility" value="private" ${visibility==='private'?'checked':''}> 🔒 Private <small>Only you</small></label><label><input type="radio" name="visibility" value="anonymous" ${visibility==='anonymous'?'checked':''}> 🕊️ Anonymous <small>Public, without your name</small></label><label><input type="radio" name="visibility" value="public" ${visibility==='public'?'checked':''}> 🌿 Public <small>Shows your pen name</small></label></div><div class="write-actions"><button class="outline-btn" onclick="saveDiary('draft')">Save draft</button><button class="submit-btn" onclick="saveDiary('published')">${id?'Save changes':'Save page'}</button></div></div>`);}
function pickMood(m){document.querySelectorAll('.mood').forEach(x=>x.classList.toggle('selected',x.dataset.mood===m))}
function setWriteMode(c){$('diaryCategory').value=c;}
async function getPrompt(){const r=await fetch('/api/prompts/random'),d=await r.json();currentPrompt=d.prompt?.text||null;$('promptBox').classList.toggle('hidden',!currentPrompt);$('promptBox').childNodes[0].textContent=currentPrompt||'';}
async function saveDiary(status){const body={title:$('diaryTitle').value,body:$('diaryBody').value,category:$('diaryCategory').value,mood:document.querySelector('.mood.selected')?.dataset.mood||null,visibility:document.querySelector('input[name="visibility"]:checked').value,prompt:currentPrompt,status};const url=editingId?`/api/diaries/${editingId}`:'/api/diaries';const r=await fetch(url,{method:editingId?'PUT':'POST',headers:{'Content-Type':'application/json',Authorization:'Bearer '+token},body:JSON.stringify(body)}),d=await r.json();if(!r.ok)return toast(d.error);closeModal();toast(status==='draft'?'Draft saved.':'Your page is saved.');showDashboard();}
async function loadDiaries(){const q=encodeURIComponent($("search")?.value||''),c=encodeURIComponent($("publicCategory")?.value||''),m=encodeURIComponent($("publicMood")?.value||'');const r=await fetch(`/api/diaries?search=${q}&category=${c}&mood=${m}`),d=await r.json();cards.innerHTML=d.diaries?.length?d.diaries.map(x=>`<article class="card story-card" onclick="readDiary(${x.id})"><div class="card-tag"><span>${date(x.created_at)}</span><span class="tag">${esc(x.category||'Thoughts')}</span></div><h3>${esc(x.title)}</h3><p>${esc(x.body).slice(0,180)}${x.body.length>180?'…':''}</p><div class="card-footer"><span>${x.is_anonymous?'🕊️ Anonymous':esc(x.author)}</span><span>${x.mood?moods[x.mood]:''}</span></div></article>`).join(''):`<div class="empty"><h3>No stories yet.</h3><p>Be the first to leave a quiet thought.</p></div>`}
async function readDiary(id){const r=await fetch('/api/diaries/'+id),d=await r.json();if(!r.ok)return toast(d.error);const x=d.diary;let bookmarked=false;if(currentUser){const b=await fetch(`/api/diaries/${id}/bookmark`,{headers:{Authorization:'Bearer '+token}});bookmarked=(await b.json()).bookmarked}openModal(`<span class="eyebrow">${esc(x.category||'Thoughts')} · ${date(x.created_at)}</span><h2>${esc(x.title)}</h2><div class="read-meta">${x.is_anonymous?'🕊️ Anonymous':esc(x.author)} ${x.mood?'· '+moods[x.mood]:''}</div>${x.prompt?`<div class="prompt-box">${esc(x.prompt)}</div>`:''}<div class="reader">${esc(x.body).replace(/\n/g,'<br>')}</div><div class="read-actions">${currentUser?`<button class="outline-btn" onclick="toggleBookmark(${x.id},${bookmarked})">${bookmarked?'★ Saved':'☆ Save page'}</button><button class="outline-btn" onclick="reportDiary(${x.id})">Report</button>`:''}</div>`)}
async function toggleBookmark(id,on){const r=await fetch(`/api/diaries/${id}/bookmark`,{method:on?'DELETE':'POST',headers:{Authorization:'Bearer '+token}});if(!r.ok)return toast('Could not update saved pages.');toast(on?'Removed from saved pages.':'Saved for later.');readDiary(id)}
async function showDashboard(){if(!currentUser)return openAuth();hideMain();document.querySelector('.section-heading .eyebrow').textContent='your journal';document.querySelector('.section-heading h2').textContent='A place that is yours.';document.querySelector('.section-heading p').textContent='Write, revisit and keep the pages that matter.';cards.innerHTML='<div class="loading">Opening your journal…</div>';const H={Authorization:'Bearer '+token};const [mr,sr,br,pr]=await Promise.all([fetch('/api/my-diaries',{headers:H}),fetch('/api/stats/me',{headers:H}),fetch('/api/bookmarks',{headers:H}),fetch('/api/profile',{headers:H})]);if(mr.status===401)return signOut();const d=await mr.json(),stats=await sr.json(),saved=await br.json(),profile=await pr.json();const visible=d.diaries.filter(x=>x.status!=='draft'),drafts=d.diaries.filter(x=>x.status==='draft');cards.innerHTML=`<div class="profile-head"><div class="avatar">${esc(currentUser.name[0]||'Q').toUpperCase()}</div><div><span class="eyebrow">your journal</span><h2>Hello, ${esc(currentUser.name)}.</h2><p>${esc(profile.bio||'A quiet place for your words.')}</p></div><button class="outline-btn" onclick="openProfile()">Edit profile</button></div><div class="stats-row"><div><strong>${stats.total}</strong><span>Pages</span></div><div><strong>${stats.currentStreak}🔥</strong><span>Current streak</span></div><div><strong>${stats.longestStreak}</strong><span>Longest streak</span></div><div><strong>${saved.diaries.length}</strong><span>Saved</span></div></div><div class="dashboard-tabs"><button class="active" onclick="renderDashboardTab('pages',this)">My pages</button><button onclick="renderDashboardTab('drafts',this)">Drafts (${drafts.length})</button><button onclick="renderDashboardTab('saved',this)">Saved (${saved.diaries.length})</button></div><div id="dashContent"></div>`;window.__dash={visible,drafts,saved:saved.diaries};renderDashboardTab('pages',document.querySelector('.dashboard-tabs button'));}
function renderDashboardTab(tab,btn){document.querySelectorAll('.dashboard-tabs button').forEach(x=>x.classList.remove('active'));btn?.classList.add('active');let items=tab==='pages'?window.__dash.visible:tab==='drafts'?window.__dash.drafts:window.__dash.saved;$("dashContent").innerHTML=`<div class="my-toolbar"><input id="mySearch" oninput="filterDash()" placeholder="Search your pages…"><select id="myCategory" onchange="filterDash()"><option value="">All categories</option>${categories.map(c=>`<option>${c}</option>`).join('')}</select><select id="myMood" onchange="filterDash()"><option value="">All moods</option>${Object.entries(moods).map(([k,v])=>`<option value="${k}">${v}</option>`).join('')}</select></div><div id="dashList" class="my-list">${renderDashList(items,tab)}</div>`;window.__dashItems=items;window.__dashTab=tab;}
function renderDashList(items,tab){return items.length?items.map(d=>`<article class="card my-card"><div class="card-tag"><span>${date(d.created_at)}</span><span class="tag">${esc(d.category||'Thoughts')}</span></div><h3>${esc(d.title)}</h3><p>${esc(d.body).slice(0,190)}${d.body.length>190?'…':''}</p><div class="card-footer"><span>${d.status==='draft'?'📝 Draft':d.is_anonymous?'🕊️ Anonymous':d.is_public?'🌿 Public':'🔒 Private'}${d.mood?' · '+moods[d.mood]:''}</span><div class="inline-actions">${tab==='saved'?`<button onclick="event.stopPropagation();toggleBookmark(${d.id},true)">Unsave</button>`:`<button onclick="event.stopPropagation();openWrite('free',${d.id},${JSON.stringify(d).replace(/"/g,'&quot;')})">Edit</button><button onclick="event.stopPropagation();deleteDiary(${d.id})">Delete</button>`}</div></div></article>`).join(''):`<div class="empty"><h3>${tab==='drafts'?'No drafts yet.':tab==='saved'?'Nothing saved yet.':'Your pages are waiting.'}</h3><p>Start with a thought, a feeling, or one sentence.</p><button class="primary-btn" onclick="openWrite()">＋ Write a page</button></div>`;}
function filterDash(){const q=$("mySearch").value.toLowerCase(),c=$("myCategory").value,m=$("myMood").value;const list=window.__dashItems.filter(d=>(!q||(d.title+' '+d.body).toLowerCase().includes(q))&&(!c||d.category===c)&&(!m||d.mood===m));$("dashList").innerHTML=renderDashList(list,window.__dashTab);}
async function deleteDiary(id){if(!confirm('Delete this page permanently?'))return;const r=await fetch('/api/diaries/'+id,{method:'DELETE',headers:{Authorization:'Bearer '+token}}),d=await r.json();if(!r.ok)return toast(d.error);toast('Page deleted.');showDashboard()}
async function openProfile(){const r=await fetch('/api/profile',{headers:{Authorization:'Bearer '+token}}),d=await r.json();openModal(`<span class="eyebrow">your profile</span><h2>How should your name feel here?</h2><p class="modal-sub">This is your public pen name. Anonymous pages never show it.</p><div class="form"><label>Pen name<input id="profileName" maxlength="40" value="${esc(d.user.name)}"></label><label>Short bio<textarea id="profileBio" rows="4" maxlength="160" placeholder="A little about your writing…">${esc(d.bio)}</textarea></label><button class="submit-btn" onclick="saveProfile()">Save profile</button></div>`)}
async function saveProfile(){const r=await fetch('/api/profile',{method:'PATCH',headers:{'Content-Type':'application/json',Authorization:'Bearer '+token},body:JSON.stringify({name:$('profileName').value,bio:$('profileBio').value})}),d=await r.json();if(!r.ok)return toast(d.error);currentUser={...currentUser,name:d.user.name};localStorage.setItem('qp_user',JSON.stringify(currentUser));closeModal();updateNav();toast('Profile updated.');showDashboard()}
function openSettings(){openModal(`<span class="eyebrow">settings</span><h2>Make Quiet Pages fit you.</h2><p class="modal-sub">These preferences stay on this device.</p><div class="settings-list"><label class="setting"><span><strong>Theme</strong><small>Choose how Quiet Pages looks.</small></span><select id="themeSelect" onchange="applyTheme(this.value)"><option value="light">Light</option><option value="dark">Dark</option><option value="system">System</option></select></label><label class="setting"><span><strong>Text size</strong><small>Make reading and writing more comfortable.</small></span><select id="fontSelect" onchange="applyFont(this.value)"><option value="normal">Normal</option><option value="large">Large</option><option value="xlarge">Extra large</option></select></label><label class="setting check"><span><strong>Reduce motion</strong><small>Use fewer animations.</small></span><input id="motionToggle" type="checkbox" onchange="applyMotion(this.checked)"></label></div>${currentUser?'<button class="outline-btn full" onclick="openProfile()">Edit profile</button>':''}`);loadSettingsUI()}
function applyTheme(v){localStorage.qp_theme=v;document.documentElement.dataset.theme=v==='system'?(matchMedia('(prefers-color-scheme:dark)').matches?'dark':'light'):v}
function applyFont(v){localStorage.qp_font=v;document.documentElement.dataset.font=v}
function applyMotion(v){localStorage.qp_motion=v?'reduce':'normal';document.documentElement.dataset.motion=v?'reduce':'normal'}
function loadSettingsUI(){if($('themeSelect'))$('themeSelect').value=localStorage.qp_theme||'light';if($('fontSelect'))$('fontSelect').value=localStorage.qp_font||'normal';if($('motionToggle'))$('motionToggle').checked=localStorage.qp_motion==='reduce'}
function reportDiary(id){if(!currentUser)return openAuth();openModal(`<span class="eyebrow">help keep it kind</span><h2>Report this page.</h2><p class="modal-sub">Reports are reviewed by the Quiet Pages admin.</p><div class="form"><label>Reason<select id="reportReason"><option>Spam</option><option>Harassment or bullying</option><option>Harmful or unsafe content</option><option>Hate or discrimination</option><option>Other</option></select></label><label>Optional details<textarea id="reportDetails" rows="4" placeholder="Tell us what concerned you…"></textarea></label><button class="submit-btn" onclick="sendReport(${id})">Send report</button></div>`)}
async function sendReport(id){const r=await fetch(`/api/diaries/${id}/report`,{method:'POST',headers:{'Content-Type':'application/json',Authorization:'Bearer '+token},body:JSON.stringify({reason:$('reportReason').value,details:$('reportDetails').value})}),d=await r.json();if(!r.ok)return toast(d.error);closeModal();toast(d.message)}
async function showAdmin(){if(!currentUser||currentUser.role!=='admin')return toast('Admin access required.');hideMain();document.querySelector('.section-heading .eyebrow').textContent='admin workspace';document.querySelector('.section-heading h2').textContent='Quiet Pages control room.';document.querySelector('.section-heading p').textContent='Moderate gently. Protect privacy. Keep the community safe.';cards.innerHTML='<div class="loading">Loading your control room…</div>';const H={Authorization:'Bearer '+token};const [s,u,e,r,a,p]=await Promise.all([fetch('/api/admin/stats',{headers:H}),fetch('/api/admin/users',{headers:H}),fetch('/api/admin/entries',{headers:H}),fetch('/api/admin/reports',{headers:H}),fetch('/api/admin/analytics',{headers:H}),fetch('/api/admin/prompts',{headers:H})]);const stats=(await s.json()).stats,users=(await u.json()).users,entries=(await e.json()).entries,reports=(await r.json()).reports,analytics=await a.json(),prompts=(await p.json()).prompts;cards.innerHTML=`<div class="admin-grid">${[[stats.users,'Users'],[stats.entries,'Entries'],[stats.anonymous,'Anonymous'],[stats.reports,'Open reports'],[stats.publicEntries,'Published'],[stats.prompts,'Active prompts']].map(x=>`<div class="admin-stat"><strong>${x[0]}</strong><span>${x[1]}</span></div>`).join('')}</div><div class="admin-panels"><section class="admin-panel"><div class="panel-head"><div><span class="eyebrow">mood pulse</span><h3>How pages are feeling</h3></div></div><div class="bar-list">${analytics.moods.filter(x=>x.mood!=='none').map(x=>`<div><span>${moods[x.mood]||x.mood}</span><b>${x.count}</b></div>`).join('')||'<p class="muted">No mood data yet.</p>'}</div></section><section class="admin-panel"><div class="panel-head"><div><span class="eyebrow">journal life</span><h3>Categories</h3></div></div><div class="bar-list">${analytics.categories.map(x=>`<div><span>${esc(x.category)}</span><b>${x.count}</b></div>`).join('')}</div></section></div><section class="admin-panel"><div class="panel-head"><div><span class="eyebrow">moderation queue</span><h3>Reports</h3></div></div>${reports.length?reports.map(x=>`<div class="admin-item"><div><strong>#${x.id} · ${esc(x.reason)}</strong><p>${esc(x.title)} · ${x.is_anonymous?'Anonymous page':'Named page'}<br>${esc(x.details||'No extra details')}</p></div><div class="admin-actions"><span class="status ${x.status}">${x.status}</span>${x.status==='open'?`<button onclick="updateReport(${x.id},'resolved')">Resolve</button><button onclick="updateReport(${x.id},'dismissed')">Dismiss</button>`:''}</div></div>`).join(''):'<p class="muted">No reports. The queue is clear.</p>'}</section><section class="admin-panel"><div class="panel-head"><div><span class="eyebrow">prompt library</span><h3>Prompts</h3></div><button class="small-btn" onclick="addPrompt()">＋ Add prompt</button></div>${prompts.map(x=>`<div class="admin-item"><div><strong>${esc(x.text)}</strong><p>${esc(x.category)} · ${x.is_active?'active':'hidden'}</p></div><div class="admin-actions"><button onclick="togglePrompt(${x.id},${!x.is_active})">${x.is_active?'Hide':'Activate'}</button><button onclick="deletePrompt(${x.id})">Delete</button></div></div>`).join('')}</section><section class="admin-panel"><div class="panel-head"><div><span class="eyebrow">content</span><h3>Recent entries</h3></div></div>${entries.slice(0,20).map(x=>`<div class="admin-item"><div><strong>${esc(x.title)}</strong><p>${x.is_anonymous?'Anonymous':'By '+esc(x.owner)} · ${date(x.created_at)} · ${esc(x.category||'Uncategorised')}</p></div><div class="admin-actions"><span class="status ${x.status}">${x.status}</span>${x.is_public&&x.status==='published'?`<button onclick="removeEntry(${x.id})">Remove</button>`:''}</div></div>`).join('')}</section><section class="admin-panel"><div class="panel-head"><div><span class="eyebrow">accounts</span><h3>Users</h3></div></div>${users.map(x=>`<div class="admin-item"><div><strong>${esc(x.name)} ${x.role==='admin'?'· Admin':''}</strong><p>${esc(x.email)} · ${x.entries} entries · joined ${date(x.created_at)}</p></div><div class="admin-actions">${x.role!=='admin'?`<button onclick="toggleUser(${x.id},${!x.is_suspended})">${x.is_suspended?'Unsuspend':'Suspend'}</button><button onclick="deleteUser(${x.id})">Delete</button>`:'<span class="status admin">protected</span>'}</div></div>`).join('')}</section>`;window.scrollTo({top:0,behavior:'smooth'})}
async function adminFetch(url,opts={}){opts.headers={...(opts.headers||{}),Authorization:'Bearer '+token,'Content-Type':'application/json'};const r=await fetch(url,opts),d=await r.json();if(!r.ok){toast(d.error||'Admin action failed.');return null}return d}
async function updateReport(id,status){if(await adminFetch(`/api/admin/reports/${id}`,{method:'PATCH',body:JSON.stringify({status})}))showAdmin()}
async function removeEntry(id){if(!confirm('Remove this entry from public view?'))return;if(await adminFetch(`/api/admin/entries/${id}`,{method:'DELETE'}))showAdmin()}
async function toggleUser(id,suspended){if(await adminFetch(`/api/admin/users/${id}`,{method:'PATCH',body:JSON.stringify({suspended})}))showAdmin()}
async function deleteUser(id){if(!confirm('Delete this user and their pages?'))return;if(await adminFetch(`/api/admin/users/${id}`,{method:'DELETE'}))showAdmin()}
async function addPrompt(){const text=prompt('Write the new journal prompt:');if(!text)return;const category=prompt('Category:','Reflection')||'Reflection';if(await adminFetch('/api/admin/prompts',{method:'POST',body:JSON.stringify({text,category})}))showAdmin()}
async function togglePrompt(id,is_active){if(await adminFetch(`/api/admin/prompts/${id}`,{method:'PATCH',body:JSON.stringify({is_active})}))showAdmin()}
async function deletePrompt(id){if(!confirm('Delete this prompt?'))return;if(await adminFetch(`/api/admin/prompts/${id}`,{method:'DELETE'}))showAdmin()}
applyTheme(localStorage.qp_theme||'light');applyFont(localStorage.qp_font||'normal');applyMotion(localStorage.qp_motion==='reduce');updateNav();loadDiaries();
