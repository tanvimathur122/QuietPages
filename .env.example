# Server settings
PORT=3000
JWT_SECRET=replace-with-a-long-random-secret

# The account using this email receives the admin role.
# Set this BEFORE creating the admin account, or restart after setting it.
ADMIN_EMAIL=you@example.com
